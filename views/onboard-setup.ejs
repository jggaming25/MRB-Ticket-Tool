<%- include('partials/head', { title, user, isHR, isHRHR, currentPath }) %>

  <div class="login-page">
    <div class="login-card">
      <div class="login-logo">🔐</div>
      <h1>Konto einrichten</h1>
      <p class="muted">Dein Discord-Konto <strong><%= user.global_name || user.username %></strong> wurde erkannt.
      Lege jetzt deine E-Mail-Adresse und ein Passwort fest.</p>

      <% if (typeof errors !== 'undefined' && errors && errors.length) { %>
        <div class="alert alert-error">
          <ul style="margin:0;padding-left:18px;">
            <% errors.forEach((e) => { %><li><%= e %></li><% }) %>
          </ul>
        </div>
      <% } %>

      <form method="post" action="/onboard/setup" class="login-form">
        <div class="form-group">
          <label for="email">E-Mail-Adresse *</label>
          <input type="email" id="email" name="email" required value="<%= values.email || '' %>">
          <p class="hint">An diese Adresse schicken wir einen Verifizierungscode.</p>
        </div>
        <div class="form-group">
          <label for="password">Passwort *</label>
          <input type="password" id="password" name="password" required minlength="8" autocomplete="new-password">
        </div>
        <div class="form-group">
          <label for="password_confirm">Passwort wiederholen *</label>
          <input type="password" id="password_confirm" name="password_confirm" required minlength="8" autocomplete="new-password">
        </div>
        <button type="submit" class="btn btn-primary btn-lg" style="width:100%;">Weiter zur Verifizierung</button>
      </form>
    </div>
  </div>

<%- include('partials/footer') %>
